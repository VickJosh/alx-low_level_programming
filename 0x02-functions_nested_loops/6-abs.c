#include "main.h"

/**
 *_abs - function prints absolute value of an integer
 *@x: function parameter
 *Return: program to return an integer value
 */

int _abs(int x)
{
	if (x < 0)
		return (-x);
	else
		return (x);
}
